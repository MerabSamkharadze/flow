import { Component } from '@angular/core';

/**
 * NotificationsComponent — placeholder page for the notifications feature.
 *
 * Will be expanded with real-time notification feed, read/unread states,
 * and notification preferences in a future phase.
 */
@Component({
  selector: 'app-notifications',
  templateUrl: './notifications.component.html',
  styleUrls: ['./notifications.component.scss'],
})
export class NotificationsComponent {}
